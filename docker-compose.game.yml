services:
  # FlappyBird 游戏
  flappybird:
    build:
      context: ./FlappyBird
      dockerfile: Dockerfile
    container_name: flappybird-game
    restart: always
    ports:
      - "3002:80"
    networks:
      - chat-network

networks:
  chat-network:
    name: ai-chat-network
    external: true
